namespace Cassam.Ui.Manager;

/// <summary>
/// Code-behind for <see cref="ManagerOverviewView"/>. Intentionally
/// empty — the overview is a static welcome card rendered from
/// XAML only.
/// </summary>
public sealed partial class ManagerOverviewView : Page
{
    public ManagerOverviewView()
    {
        this.InitializeComponent();
    }
}